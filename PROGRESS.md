# Progress

## 2026-09-15

- 初始化项目进度记录。
- 已检查 Maven 配置、源码结构及测试结构。
- 当前默认 Maven 使用 JDK 23，直接运行 `mvn clean test` 会被 Enforcer 拒绝；项目要求 JDK 8。
- 使用本机 Zulu JDK 8 执行 `mvn clean test`：主代码与测试代码编译成功，36 个测试中 32 个通过、4 个失败。
- 已确认 4 个失败的根因：
  - `FishUI.win()` 用 `m/s` 参与排行榜元素交换，导致当前游戏时间被覆盖。
  - `FishUI.win()` 用“分钟和秒都非零”判断有效记录，错误地把 `1:00` 视为空记录。
  - `FishUI.gameTime()` 只格式化显示，没有把 60 秒进位成 1 分钟。
  - `ImagePool.getImage()` 没有索引边界检查，索引 20 会抛出越界异常。
- 本次按“构建并排查”范围完成诊断，未修改业务代码。
- 已接入深海主题中文 UI：主菜单、暂停菜单、存档和排行榜按钮保留既有动作命令，改为深海文案与样式；排行榜使用 `no1.png`—`no5.png` 的名次勋章和装饰，存档面板使用 `BG1.png`，胜负结算补上“深渊主宰 / 潜航失败”中文标题，HUD 改为“能量 / 潜航时间”。
- 使用 Zulu JDK 8 执行 `mvn -B -ntp clean test`：业务和测试源码均编译通过，36 条中 32 条通过、4 条失败；失败仍为既有排行榜记录、时间归一化和非法图片索引缺陷，未由本次 UI 改动引入。
- 重写 `README.md`，补充项目结构、JDK 8/Maven 配置、IDE 启动、命令行与一键运行方式、资源相对路径约束、测试隔离机制和当前已知缺陷。
- 新增 `run-game.command`、`run-tests.command` 和共用的 `scripts/java8-env.sh`；脚本可自动选择 macOS 上的 JDK 8，且测试脚本保留 Maven 真实退出码。
- 在 macOS、Azul Zulu JDK 1.8.0_502、Maven 3.9.9 下重新验证：游戏编译成功，`fish.GameFrame` 进程与 GUI 窗口创建成功；全量 36 条测试为 32 通过、4 失败、0 错误、0 跳过，失败与已知缺陷一致。
