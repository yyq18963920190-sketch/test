# TODO

- [x] 执行干净构建并记录失败现象。
- [x] 定位构建失败根因。
- [x] 补充项目结构、环境配置和测试运行 README。
- [x] 补充并验证 macOS 游戏与测试一键脚本。
- [ ] 修复 `FishUI.win()` 的整分钟记录判断和时间变量覆盖。
- [ ] 修复 `FishUI.gameTime()` 的 60 秒归一化。
- [ ] 为 `ImagePool.getImage()` 增加索引边界保护。
- [ ] 修复后使用 JDK 8 重新执行 `mvn clean test`。
