# A-01 公共测试基础配置实施记录

本批只接入公共测试基座及隔离资源配置，不包含 A-02 或后续批次的业务测试。

## 授权与来源

用户明确指定：仅完成 A-01；分支为 `feat：公共测试基础配置`（全角冒号 U+FF1A）；本地 commit message 为 `添加公共测试基础配置与隔离资源`；验证后本地提交，不 push、不合并，由用户手动合并。

实现依据同级 `提交文件/AB测试代码分批提交交接.md` 的 A-01。公共基类为 AI 辅助适配实现，参考往届备份 `myTest/CollisionTest.java` 的对象初始化和 `myTest/WinTest.java` 的排行榜准备方式，新增 EDT 调度、工作目录检查及副本恢复。它不是旧文件原样恢复，也不表示个人独立原创或已完成业务测试贡献。Codex 在本次授权下完成接入和下述验证，未修改 Git 作者或时间。

## 本批内容

- `pom.xml`：JUnit 4.13.2，固定版本的 Enforcer/Compiler/Resources/Surefire；要求 Maven 使用 JDK 8，单 fork，headless，测试工作目录为 `target/ab-test-work`。
- `src/test/java/handoff/support/AbTestBase.java`：共享初始化、EDT 执行规则、图片检查、排行榜副本准备与恢复。
- `src/test/resources/ab/ranking/empty.txt`：三行 `0:0`。
- `.gitignore`：原规则忽略所有 `*.txt`，仅放行 `src/test/resources/ab/ranking/*.txt`，使测试夹具进入版本管理。
- 本记录：保存授权范围、AI/往届来源和实际验证信息，不接入 A-05 的全量运行说明。

## 实际验证

验证日期：2026-09-14；macOS 27.0 / aarch64；Maven 3.9.9；Azul Zulu JDK 1.8.0_502。

本次使用独立 JDK，不修改系统默认 Java。下载来源：
[Azul 官方 JDK 8 压缩包](https://cdn.azul.com/zulu/bin/zulu8.96.0.19-ca-jdk8.0.502-macosx_aarch64.tar.gz)。
压缩包 SHA-256：`9f9e5038c638e415e507e8b5118a774822f553a56e76bdf4b042c3fbe7b69083`。

从工程根目录执行的编译命令：

```sh
JAVA_HOME='/Users/yzq/Desktop/资料/软件测试课程/tmp/a01/jdk8/zulu8.96.0.19-ca-jdk8.0.502-macosx_aarch64/Contents/Home' /opt/homebrew/bin/mvn -B -ntp -DskipTests clean test-compile
```

结果：退出码 0，`BUILD SUCCESS`。编译 6 个游戏源文件和 1 个公共测试源文件；原有 ActListener 使用过时 API 的警告仍存在。复制 21 份隔离资源（20 张图片和 1 份排行榜），并复制 1 份空榜测试夹具。

文件核对：21 份隔离资源与对应原文件逐字节一致；公共基类 class 已生成；测试 classpath 中的 empty.txt 与源夹具一致。运行前后的游戏源文件及业务资源 SHA-256 一致。

额外配置检查：

| 检查 | 实际结果 | 解释 |
| --- | --- | --- |
| 现有 Maven 默认 JDK 23 执行 `mvn -B -ntp validate` | 退出码 1，要求使用 JDK 8 | 版本限制按预期阻止错误环境 |
| JDK 8 执行 `mvn -B -ntp test` | 退出码 1，`No tests were executed!` | A-01 没有业务测试；零用例保护按预期生效，不是测试通过 |
| POM XML、文件范围及 Git 差异检查 | 通过 | 无后续批次的测试类；原 CRLF 文件按 `git -c core.whitespace=cr-at-eol diff --check` 检查 |

原始命令日志保留在同级 `tmp/a01/` 的 `java8-test-compile.log`、`non-java8-validation.log`、`no-tests-guard.log`；日志和 JDK 下载物不纳入 Git。

## 验证边界

本批业务测试执行数为 0。没有运行最终36条用例、启动游戏或验证候选缺陷；公共规则在实际业务测试中的 EDT 执行和异常恢复流程尚待后续用例接入验证。本次已验证的是编译、资源复制、数据一致性和配置保护，不宣称全量测试通过。工作停在 A-01，后续由用户手动合并再安排其他批次。
